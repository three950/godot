# Godot 内建 Drag & Drop 系统使用说明

## 概述

已将原有的手动拖拽系统改造为使用 Godot 内建的 Drag & Drop 系统。这个系统的优势：

✅ **完美解决背包删除问题**：拖拽时使用的是数据引用而非节点移动，背包关闭时不会误删正在拖拽的卡片
✅ **代码更简洁**：不需要手动管理 z_index、父节点转换
✅ **更好的性能**：引擎级别的优化
✅ **扩展性强**：轻松支持"拖到空白处丢弃"等高级功能

## 实现文件

### 1. `card.gd` - 卡片脚本

核心方法：
- `_get_drag_data(at_position)` - 开始拖拽时返回拖拽数据
- `_can_drop_data(at_position, data)` - 判断是否可以堆叠
- `_drop_data(at_position, data)` - 执行堆叠操作
- `create_drag_preview()` - 创建半透明拖拽预览

### 2. `bag_slot.gd` - 卡槽脚本

核心方法：
- `_can_drop_data(at_position, data)` - 判断是否可以放入槽位
- `_drop_data(at_position, data)` - 将卡片放入槽位
- `place_card(card)` - 放置卡片
- `remove_card()` - 移除卡片

### 3. `bag.gd` - 背包面板脚本

辅助方法：
- `get_all_slots()` - 获取所有卡槽
- `get_empty_slots()` - 获取空卡槽
- `get_all_cards()` - 获取背包中所有卡片
- `is_full()` - 检查背包是否已满

## 重要修改说明

### ⚠️ 需要在场景中修改

由于 Godot 的 Drag & Drop 系统通过 Control 节点的方法触发，而卡片场景中使用了 Button 来处理输入，需要做以下调整：

**选项 A：移除 Button（推荐）**

1. 打开 `card.tscn`
2. 删除 Button 节点
3. 在根节点 `Card_莉可` (Control) 的检查器中：
   - 找到 "Mouse" 部分
   - 设置 `Mouse Filter` 为 `Stop`（默认值）
   - 确保 `Mouse Default Cursor Shape` 为 `Arrow` 或 `Drag`

**选项 B：保留 Button，修改鼠标过滤**

1. 打开 `card.tscn`
2. 选择 Button 节点
3. 在检查器中设置：
   - `Mouse Filter` 改为 `Pass`
   - 断开 `button_down` 和 `button_up` 信号连接（或保留用于其他功能）

## 工作流程

### 拖拽开始
```
1. 用户点击卡片并拖动
2. Godot 调用 card._get_drag_data()
3. 返回拖拽数据字典（包含卡片引用）
4. 创建半透明预览图
5. 清除原位置的引用（但不删除节点）
```

### 拖拽中
```
1. 预览图跟随鼠标
2. 真实卡片节点保持在原父节点下（不可见或透明）
3. 当鼠标移到可接受的目标上时，目标会高亮
```

### 拖拽结束（放到卡槽）
```
1. Godot 调用 bag_slot._can_drop_data()
2. 如果返回 true，调用 bag_slot._drop_data()
3. 卡片被移动到卡槽节点下
4. 设置卡片位置和缩放
```

### 拖拽结束（堆叠到卡片）
```
1. Godot 调用 target_card._can_drop_data()
2. 如果返回 true，调用 target_card._drop_data()
3. 设置堆叠关系
4. 调整卡片位置和层级
```

### 拖拽结束（空白处）
```
1. 如果没有任何目标接受拖放
2. 拖拽自动取消
3. 卡片保持在拖拽前的位置
```

## 处理背包关闭时的拖拽

**关键优势**：

使用新系统后，当背包关闭/删除时：
- 拖拽预览不会消失（由引擎管理）
- 拖拽数据（卡片引用）仍然有效
- 用户可以继续将卡片拖到其他目标
- 卡片节点不会被误删

**旧系统的问题**：
- 卡片是背包的子节点
- 背包删除时，所有子节点一起删除
- 需要在删除前手动转移父节点（复杂且容易出错）

## 扩展功能

### 添加"丢弃到空白处"功能

在 CardManager 或主场景中添加：

```gdscript
extends Control

func _can_drop_data(_at_position: Vector2, data) -> bool:
    # 接受所有卡片拖放
    if typeof(data) == TYPE_DICTIONARY and data.has("card"):
        return true
    return false

func _drop_data(at_position: Vector2, data) -> void:
    if typeof(data) != TYPE_DICTIONARY or not data.has("card"):
        return
    
    var card = data["card"]
    
    # 将卡片移动到主场景
    if card.get_parent() != self:
        var old_parent = card.get_parent()
        if old_parent:
            old_parent.remove_child(card)
        add_child(card)
    
    # 设置位置
    card.global_position = at_position - card.size / 2
    card.cardCurrentState = card.cardState.fixed
    
    print("卡片 %s 被丢弃到位置 %s" % [card.name, at_position])
```

### 添加视觉反馈

在卡片或卡槽中添加：

```gdscript
func _can_drop_data(at_position: Vector2, data) -> bool:
    var can_drop = # ... 检查逻辑
    
    # 高亮显示可接受的目标
    if can_drop:
        modulate = Color(1, 1, 0.5)  # 淡黄色
    else:
        modulate = Color(1, 0.5, 0.5)  # 淡红色
    
    return can_drop

func _notification(what: int) -> void:
    if what == NOTIFICATION_DRAG_END:
        # 拖拽结束后恢复颜色
        modulate = Color.WHITE
```

## 调试技巧

1. **检查拖拽数据**：在 `_get_drag_data` 中打印返回值
2. **验证目标接受**：在 `_can_drop_data` 中打印判断逻辑
3. **跟踪节点父级**：拖放后检查 `card.get_parent()`
4. **查看场景树**：在编辑器的 Remote 标签页中实时查看节点关系

## 常见问题

**Q: 拖拽没有反应？**
- 检查 Control 节点的 `Mouse Filter` 是否为 `Stop`
- 确保 `_get_drag_data` 返回非 null 值
- 检查是否有 Button 拦截了鼠标事件

**Q: 预览图不显示？**
- 确保 `create_drag_preview()` 返回有效的 Control 节点
- 预览节点必须有可见的子节点

**Q: 无法放到目标上？**
- 检查目标的 `_can_drop_data` 是否返回 true
- 确保目标的 `Mouse Filter` 允许接收输入

**Q: 卡片位置不对？**
- 在 `_drop_data` 中使用 `global_position` 而非 `position`
- 注意父节点转换时要保存全局位置

## 总结

新的 Drag & Drop 系统完美解决了背包关闭时卡片被误删的问题，同时代码更简洁、更易维护。只需在场景中稍作调整（移除或修改 Button），即可享受引擎级别的拖放支持。

