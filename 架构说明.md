# 游戏数据架构重构说明

## 📋 概览

本次重构将原有的"每次运行时读取CSV"的方式，改为"使用Resource类 + 全局单例"的优秀架构模式。

## 🏗️ 新架构组成

### 1. CharacterData Resource 类 (`character_data.gd`)

**作用**：定义角色数据的结构，提供类型安全的数据存储。

**主要功能**：
- ✅ 使用 `@export` 使数据可在编辑器中编辑
- ✅ 提供 `from_dict()` 静态方法从CSV数据创建实例
- ✅ 提供 `to_dict()` 方法将数据序列化
- ✅ 支持扩展属性（稀有度、成本等）

**优势**：
- 类型安全：编辑器有智能提示
- 可复用：可在整个项目中统一使用
- 易扩展：添加新属性只需修改一处

### 2. GameData 全局单例 (`game_data.gd`)

**作用**：全局数据管理器，负责加载和存储所有游戏数据。

**主要功能**：
- 在游戏启动时自动加载所有数据（仅一次）
- 提供全局访问接口：
  - `get_character(name)` - 获取单个角色
  - `get_all_characters()` - 获取所有角色
  - `has_character(name)` - 检查角色是否存在
- 数据缓存，无需重复读取文件

**配置**：
已在 `project.godot` 中配置为 Autoload：
```ini
[autoload]
GameData="*res://game_data.gd"
```

### 3. 重构后的 CardManager (`card_manager.gd`)

**改进点**：
- ❌ 删除了 `load_character_data_from_csv()` 方法
- ✅ 直接从 `GameData` 单例获取数据
- ✅ 代码更简洁，职责更清晰
- ✅ 使用 `CharacterData` 类型，享受类型检查

## 📊 数据流程对比

### 旧架构（之前）
```
游戏运行 → CardManager._ready()
         → 读取CSV文件
         → 解析为Dictionary
         → 生成卡片
```
**问题**：
- 每次运行都要读取文件
- 数据分散，难以复用
- 无类型安全

### 新架构（现在）
```
游戏启动 → GameData._ready()（自动运行）
         → 读取CSV文件（仅一次）
         → 转换为CharacterData对象
         → 存储在内存中

其他脚本 → 通过GameData获取数据
         → 直接使用已加载的数据
```
**优势**：
- ✅ 数据只加载一次
- ✅ 全局统一访问
- ✅ 类型安全
- ✅ 易于扩展和维护

## 🚀 使用示例

### 在任何脚本中获取角色数据

```gdscript
# 获取单个角色
var riko_data = GameData.get_character("莉可")
if riko_data:
    print("莉可的生命值: ", riko_data.max_hp)
    print("莉可的攻击力: ", riko_data.atk)

# 获取所有角色
var all_chars = GameData.get_all_characters()
for char in all_chars:
    print(char.character_name, ": HP=", char.max_hp)

# 检查角色是否存在
if GameData.has_character("雷古"):
    print("雷古存在！")
```

### 在编辑器中创建角色数据（未来扩展）

将来可以直接在Godot编辑器中创建 `.tres` 资源文件：

1. 右键 → 新建资源 → CharacterData
2. 在Inspector中直接编辑属性
3. 保存为 `角色_莉可.tres`
4. 在GameData中加载Resource文件

## 🎯 后续优化方向

### 短期（可选）
1. 添加编辑器工具，自动同步CSV到Resource文件
2. 支持热重载，开发时修改数据无需重启

### 中期（推荐）
1. 将CSV数据导出为 `.tres` 资源文件
2. 分离不同类型的数据（角色、装备、道具等）
3. 添加数据验证和错误处理

### 长期（高级）
1. 使用JSON或自定义格式替代CSV
2. 实现数据版本控制
3. 支持数据加密和压缩
4. 实现资源流式加载（适用于大型游戏）

## 📝 注意事项

1. **启动顺序**：GameData 作为 Autoload 会在场景之前初始化
2. **异步加载**：CardManager 使用 `await` 确保数据已加载
3. **数据修改**：运行时修改 CharacterData 对象会影响所有引用
4. **CSV文件**：仍保留为数据源，方便策划编辑

## 🎉 重构成果

- ✅ 代码更清晰，职责分明
- ✅ 性能更好，数据只加载一次
- ✅ 类型安全，减少运行时错误
- ✅ 易于扩展，符合优秀游戏设计模式
- ✅ 全局访问，方便在任何地方使用数据

---

**总结**：这是一个符合游戏行业最佳实践的数据管理架构！

