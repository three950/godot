@shop_manager.gd 添加一个全局的coin变量。“探窟者协会”改成只能接收value的卡片堆叠。在全局设定一个coin变量显示在shop区域右边。当把有value属性的卡片堆叠到“探窟者协会”这张卡时，把卡片（组合）销毁，每一项的value相加加到coin变量上

@character_card.tscn 实现一个常见的悬停UI逻辑。在人物卡片的右侧加一个小区域，当鼠标在上面悬停超过0.5s时，在区域右侧显示背包区域@bag.tscn 如果鼠标离开小区域后0.5s内鼠标位置处在新出现的背包区域内，那么这个背包区域就不消失。直到鼠标移出该区域才消失

HoverVisual 的鼠标过滤器可能阻止了鼠标事件